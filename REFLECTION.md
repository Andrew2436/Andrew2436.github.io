
The difference between flex-direction: row and flex-direction: column is that one aligns items inside a container horizontally while the other aligns items inside the container vertically.


It is important to use relative units instead of fixed pixels to adjust to cases where users are accessing websites and pages through different devices. Relative units ensure adaptability across devices leading to less issues.

Only used AI for syntax errors and to remember some functions.